The Softmax function takes a vector of raw scores (logits) and applies the transformation to each score using SoftMax function. This function exponentiates each score and normalizes by the sum of all exponentiated scores, yielding a probability distribution over the classes.